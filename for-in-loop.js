const person = {
    name: 'Adesuwa',
    age: 28
};
for(let key in person)
    console.log(key, person[key]);


const colors = ['blue', 'yellow', 'green', 'brown'];
for (let shade in colors)
    console.log(colors[shade]);